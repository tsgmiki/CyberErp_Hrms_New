# Production environment variables
VITE_API_BASE_URL=http://meftuhhamid-001-site4.jtempurl.com/api/
VITE_APP_NAME=Cyber HRMS
# System manual PDF. Default: /manuals/user-manual.pdf, served from frontend/public/manuals/ —
# drop the PDF there and it is live with no rebuild. Set this to rename it, or to point at one
# shared manual served from anywhere.
VITE_MANUAL_URL=/manuals/user-manual.pdf
