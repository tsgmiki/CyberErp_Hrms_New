import { memo } from "react";
import CriticalPosition from "@/components/admin/criticalPosition";
function CriticalPositionPage() { return <CriticalPosition />; }
export default memo(CriticalPositionPage);
