import { memo } from "react";
import TrainingSession from "@/components/admin/trainingSession";

function TrainingSessionPage() {
  return <TrainingSession />;
}

export default memo(TrainingSessionPage);
