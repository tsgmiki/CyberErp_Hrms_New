import { memo } from "react";
import SalaryRevision from "@/components/admin/salaryRevision";

function SalaryRevisionPage() {
  return <SalaryRevision />;
}

export default memo(SalaryRevisionPage);
