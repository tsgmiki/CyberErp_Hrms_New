import { memo } from "react";
import MedicalProvider from "@/components/admin/medicalProvider";

function MedicalProviderPage() {
  return <MedicalProvider />;
}

export default memo(MedicalProviderPage);
