import { memo } from "react";
import TrainingCertificate from "@/components/admin/trainingCertificate";

function TrainingCertificatePage() {
  return <TrainingCertificate />;
}

export default memo(TrainingCertificatePage);
