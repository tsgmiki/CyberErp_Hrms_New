import { memo } from "react";
import PositionCompetency from "@/components/admin/positionCompetency";

function PositionCompetencyPage() {
  return <PositionCompetency />;
}

export default memo(PositionCompetencyPage);
