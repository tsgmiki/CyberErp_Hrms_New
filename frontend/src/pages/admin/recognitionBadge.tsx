import { memo } from "react";
import RecognitionBadge from "@/components/admin/recognitionBadge";

function RecognitionBadgePage() {
  return <RecognitionBadge />;
}

export default memo(RecognitionBadgePage);
