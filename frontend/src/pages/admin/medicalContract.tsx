import { memo } from "react";
import MedicalContract from "@/components/admin/medicalContract";

function MedicalContractPage() {
  return <MedicalContract />;
}

export default memo(MedicalContractPage);
