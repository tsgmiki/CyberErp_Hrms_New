import { memo } from "react";
import EmployeeCompensation from "@/components/admin/employeeCompensation";

function EmployeeCompensationPage() {
  return <EmployeeCompensation />;
}

export default memo(EmployeeCompensationPage);
