import { memo } from "react";
import ReportDefinition from "@/components/admin/reportDefinition";

function ReportDefinitionPage() {
  return <ReportDefinition />;
}

export default memo(ReportDefinitionPage);
