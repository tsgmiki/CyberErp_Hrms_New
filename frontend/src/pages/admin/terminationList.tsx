import { memo } from "react";
import TerminationList from "@/components/admin/terminationList";

function TerminationListPage() {
  return <TerminationList />;
}

export default memo(TerminationListPage);
