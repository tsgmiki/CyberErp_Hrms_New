import { memo } from "react";
import NotificationTemplate from "@/components/admin/notificationTemplate";

function NotificationTemplatePage() {
  return <NotificationTemplate />;
}

export default memo(NotificationTemplatePage);
