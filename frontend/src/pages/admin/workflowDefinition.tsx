import { memo } from "react";
import WorkflowDefinition from "@/components/admin/workflowDefinition";

function WorkflowDefinitionPage() {
  return <WorkflowDefinition />;
}

export default memo(WorkflowDefinitionPage);
