import { memo } from "react";
import CompensationRequest from "@/components/admin/compensationRequest";

function CompensationRequestPage() {
  return <CompensationRequest />;
}

export default memo(CompensationRequestPage);
