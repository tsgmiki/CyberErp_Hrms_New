import { memo } from "react";
import MedicalEnrollment from "@/components/admin/medicalEnrollment";

function MedicalEnrollmentPage() {
  return <MedicalEnrollment />;
}

export default memo(MedicalEnrollmentPage);
