import { memo } from "react";
import InsurancePolicy from "@/components/admin/insurancePolicy";

function InsurancePolicyPage() {
  return <InsurancePolicy />;
}

export default memo(InsurancePolicyPage);
