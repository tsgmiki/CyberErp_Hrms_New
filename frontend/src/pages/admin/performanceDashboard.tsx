import { memo } from "react";
import PerformanceDashboard from "@/components/admin/performanceDashboard";

function PerformanceDashboardPage() {
  return <PerformanceDashboard />;
}

export default memo(PerformanceDashboardPage);
