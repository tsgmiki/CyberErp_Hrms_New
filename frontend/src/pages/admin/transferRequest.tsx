import { memo } from "react";
import TransferRequest from "@/components/admin/transferRequest";

function TransferRequestPage() {
  return <TransferRequest />;
}

export default memo(TransferRequestPage);
