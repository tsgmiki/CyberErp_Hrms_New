
- [SRMS schema audit](srms-schema-audit.md) — 2026-08-15: all 30 SRMS tables exist in CERP; full column diff (name/type/size/nullability/DEFAULT) = 65 → 50. TenantRole.SourceTemplateId→`RoleId` column (HasColumnName) ⇒ TenantRole ZERO. ⚠️ Blanket `datetime2(3)` convention change fixed 16 / **broke 13** (SRMS keeps (7) on Person/SalaryScale/Step/SubscriptionPlan/Tenant/TenantSubscription/UserPreference) — rolled back a 594-column migration, replaced with an explicit 17-entity list. **Verify the target's shape before generalising.** ⚠️ Remaining 50 are NOT a to-do list: 10 `TenantId`-absent are load-bearing isolation, 5 `TenantId`-nvarchar would REVERSE the §12.14 re-key, `Subsystem.Url` drop would break the Home launcher, `User.CreatedAt` REGRESSED in SRMS (I fixed it 08-14). Only the ~25 default-constraint diffs are mechanical.
